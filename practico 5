main
#include <Triangulo>
#include <iostream>
using namespace std;
int main()
{
	float base, altura;
	Triangulo triangulito;
	cout << "Ingresar la base:";
	cin >> base;
	cout << "Imgresar la altura:";
	cin >> altura;
	cout << "El area es:" << triangulito.calculararea(base, altura);
	return 0;
}
triangulo cpp
#include "Triangulo.h"
Triangulo:: Triangulo() {
}
Triangulo::~Triangulo() {
}
float Triangulo: :calculararea(float b, float h) {
	return (b * h) / 2;
}
triangulo.h
#pragma once
class Triangulo
{
private:
	float base;
	float altura;
public:
	Triangulo();
	~Triangulo();
	float calculararea(float b, float h);
};

